image: Ubuntu

stack: jdk 1

branches:
  only:
    - main

install:
  - java -jar ./artifacts/app-ibank.jar -P:profile=test &

build: off

build_script:
  - chmod +x gradlew
  - ./gradlew test --info -Dselenide.headless=true